<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Connexion — Agrinova</title>
    <link rel="stylesheet" href="{{ asset('css/agrinova.css') }}">
    <style>
        /* ── Auth page specific ── */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow: hidden;
        }

        /* Floating depth orbs */
        .orb {
          