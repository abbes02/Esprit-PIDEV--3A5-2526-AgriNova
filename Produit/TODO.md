# Task Complete ✅

## Created:
- `src/Entity/` + `.gitignore`
- `src/Repository/` + `.gitignore`
- `templates/` (root, Symfony standard) + `.gitignore` + `base.html.twig`

## Ready! Test:
```

cd Produit
symfony serve
```
http://127.0.0.1:8001 (or port shown) shows home page

Install deps:
```
composer require maker-bundle --dev
```
`make:entity Product`.
