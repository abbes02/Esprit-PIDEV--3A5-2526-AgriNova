<form method="post" action="{{ path('app_panne_delete', {'id': panne.id, 'user_id': current_user.id}) }}" onsubmit="return confirm('Delete this panne record?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_panne_' ~ panne.id) }}">
    <button class="button danger">Delete</button>
</form>
