{% extends 'parcelle/base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<section class="page-panel reveal">
    <div class="section-head">
        <div>
            <div class="section-kicker"><i class="bi bi-pencil-square"></i> Formation</div>
            <h1 class="page-title reveal-text">{{ title }}</h1>
        </div>
        <a href="{{ path('formation_index') }}" class="button-secondary">Retour</a>
    </div>

    <div class="form-shell">
        {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
        <div class="row g-3">
            <div class="col-12">{{ form_row(form.titre) }}</div>
            <div class="col-md-6">{{ form_row(form.domaine) }}</div>
            <div class="col-md-6">{{ form_row(form.niveau) }}</div>
            <div class="col-12">{{ form_row(form.description) }}</div>
            <div class="col-md-4">{{ form_row(form.dureeHeures) }}</div>
            <div class="col-md-4">{{ form_row(form.prix) }}</div>
            <div class="col-md-4">{{ form_row(form.dateCreation) }}</div>
            <div class="col-md-6">{{ form_row(form.statut) }}</div>
        </div>

        <div class="stack-sm mt-4">
            <button class="button-primary" type="submit">Enregistrer</button>
            <a href="{{ path('formation_index') }}" class="button-secondary">Annuler</a>
        </div>
        {{ form_end(form) }}
    </div>
</section>
{% endblock %}
