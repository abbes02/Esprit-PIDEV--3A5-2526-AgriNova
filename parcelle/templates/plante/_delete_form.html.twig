<form method="post" action="{{ path('app_plante_delete', {'idPlante': plante.idPlante}) }}" onsubmit="return confirm('Supprimer cette plante ?');" class="d-inline">
    <button type="submit" class="{{ button_class|default('button-danger') }}">Supprimer</button>
</form>
